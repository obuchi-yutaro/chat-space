.sidebar
  .sidebar_contents
    .user
      .user_information
        .user_name
          taro
        .font_awesome
          = fa_icon("edit")
          = fa_icon("cog")
    .box
      - @messages.each do |message|
        .comment_box
          #{message.text}
      